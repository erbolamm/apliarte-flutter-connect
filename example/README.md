# apliarte_flutter_connect example

Minimal example app for the `apliarte_flutter_connect` development preview.

This example intentionally shows package metadata and basic initialization only.
Real mDNS/Bonjour discovery, WebSocket LAN transport, and frame streaming are planned for later milestones.

## Run

```bash
cd example
flutter pub get
flutter run
```
